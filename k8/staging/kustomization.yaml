apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tableland-staging

resources:
- ../base

configMapGenerator:
- envs:
  - configApi.env
  name: configmap-api
- envs:
  - configHealthbot.env
  name: configmap-healthbot
- files:
  - config-validator-staging.json
  name: configmap-validator-config
- files:
  - config-healthbot-staging.json
  name: configmap-healthbot-config


patchesStrategicMerge:
- ingress-nginx-patch.yaml
